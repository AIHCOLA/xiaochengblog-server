export interface ServiceConfig {
    /** 是否启用降级模式（不强制要求 Cookie） */
    fallbackMode: boolean;
    /** 是否使用全局 Cookie（向后兼容） */
    useGlobalCookie: boolean;
    /** Cookie 参数名称（支持从 query 或 header 传递） */
    cookieParamName: string;
}
export declare const normalizeServiceConfig: (value: unknown) => ServiceConfig;
declare let config: ServiceConfig;
export default config;
