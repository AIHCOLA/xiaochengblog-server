#!/usr/bin/env node
import { n as startServer, r as app } from "./server-CYpOaKm7.js";
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";
//#region src/app.ts
var resolveRealPath = (filePath) => {
	try {
		return fs.realpathSync.native(filePath);
	} catch {
		return path.resolve(filePath);
	}
};
if ((process.argv[1] ? resolveRealPath(process.argv[1]) : "") === resolveRealPath(fileURLToPath(import.meta.url))) startServer();
var app_default = app;
//#endregion
export { app_default as default };
